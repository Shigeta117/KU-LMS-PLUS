## 改善案 実施状況

---

### UX / 操作性

1. ✅ EmptyState のメッセージ修正 → 「拡張機能またはブックマークレットを実行してください」に変更
2. ✅ ログアウトボタンをヘッダーから削除 → 設定ページのみに集約
3. ✅ カテゴリフィルタのスクロール視覚化 → 右端グラデーションフェードを追加
4. ✅ タブ切り替えでカテゴリがリセットされる → タブ別にカテゴリ状態を保持するように変更
5. ✅ 誤タップ時の Undo がない → 完了/非表示操作後に「元に戻す」付きトーストを表示（4秒）
6. ✅ 期限切れ課題が未完了タブに混在 → pending タブで「期限切れ」「期限内」のセクションヘッダーで分離
7. ✅ formatRelativeDeadline のエッジケース → 「0分」→「まもなく」、「0時間超過」→「期限超過」に修正
8. ✅ 完了+非表示の複合状態が不透明 → 完了済み課題には「非表示」ボタンを非表示にして複合状態を防止

   補足: 授業資料・LTIツールの別タブ表示は今後の追加課題として残す

---

### 機能

9.  ✅ Supabase Realtime → assignments テーブルを購読して拡張機能UPSERT後に自動反映
10. ✅ deleteAssignment デッドコード削除 → supabase.ts から関数を削除
11. ✅ Service Worker 追加 → public/sw.js でオフラインフォールバック、ThemeProvider から登録
12. ✅ Sign Up UI を削除 → ログインフォームのみに絞る
13. ⏭ 拡張機能のログイン状態をWebアプリに表示 → chrome.runtime 連携が必要で工数大、保留

---

### コード保守性

14. ✅ content.js / bookmarklet.js の同期注記 → 両ファイルに NOTE コメントを追加
15. ✅ appleWebApp.title の "KMS+" → "LMS+" リネーム漏れ修正
16. ⏭ Supabase 認証情報の二重管理 → bookmarklet.js はブラウザ直実行のため構造上やむを得ない
17. ✅ slide-up キーフレーム未定義 → globals.css に @keyframes slide-up を追加
18. ✅ select('*') を明示的フィールド指定に変更
19. ✅ AuthGuard の二重管理を整理 → onAuthStateChange のみに統一（getSession を削除）
20. （page.tsx のモノリシック構成は Realtime 追加等で変更済み。大規模リファクタは今後）

---

### PWA / manifest

21. ✅ manifest.ts の icons purpose → any / maskable を2エントリに分けて設定
22. （apple-touch-icon 180x180 専用ファイルは既存アイコン素材次第、素材が揃い次第対応）

---

### セキュリティ

23. （bookmarklet Anon Key の文書化 → RLS 設定の docs 整備は今後）
24. ✅ background.js の配布用コメント削除

---

### 追加要望

- ✅ ダークモード手動切り替え → 設定ページに システム/ライト/ダーク の3択を追加
- ✅ 利用規約・プライバシーポリシーページ → /legal ページを新設、設定ページからリンク

---

### コミット一覧

| コミット | 内容 |
|---------|------|
| 5e0b2f3 | 小規模な修正まとめて（1,2,7,10,12,14,15,17,18,19,21,24） |
| 090a8a8 | UX改善（3,4,5,6,8） |
| d100fb4 | ダークモード手動切り替え |
| 4d1b826 | Supabase Realtime（9） |
| ec5e1b4 | Service Worker（11） |
| 85d9da5 | 利用規約・プライバシーポリシー |
